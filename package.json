{
  "name": "color",
  "version": "1.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "pnpm build && wrangler deploy",
    "cf:dev": "wrangler dev --local --port 8787"
  },
  "devDependencies": {
    "vite": "^8.0.0",
    "wrangler": "^4.73.0"
  }
}
