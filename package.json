{
  "name": "color",
  "version": "1.0.1",
  "type": "module",
  "engines": {
    "node": ">=24.0.0",
    "pnpm": ">=10.0.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "pnpm build && wrangler deploy",
    "cf:dev": "wrangler dev --local --port 8787"
  },
  "devDependencies": {
    "vite": "^8.0.0",
    "wrangler": "^4.74.0"
  }
}
